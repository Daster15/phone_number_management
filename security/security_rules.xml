<odoo>
    <record id="number_pool_user_rule" model="ir.rule">
        <field name="name">Number Pool User</field>
        <field name="model_id" ref="model_number_pool"/>
        <field name="domain_force">[(1,'=',1)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <!-- Similar rules for other models -->
</odoo>